---
import Button from '../ui/Button.astro';
---

<div class="slide slide-hero swiper-slide swiper-slide-active">
  <div class="slide-background"></div>

  <div class="slide-content">
    <div class="slide-header">
      <h1 class="title">
        Vibe Coding vs<br />
        <span class="highlight">Software Engineering</span>
      </h1>
      <p class="subtitle">
        El equilibrio entre velocidad y calidad en la era de la IA
      </p>
    </div>

    <div class="intro-content">
      <div class="author-info card-glass">
         <p class="author-name">Alejandro</p>
         <p class="author-role">Software Engineer & AI Architect</p>
         <p class="author-context">Explorando buenas prácticas en era de IA</p>
       </div>
    </div>

    <div class="cta-section">
      <Button variant="primary" class="explore-btn">
        Explorar la paradoja →
      </Button>
    </div>
  </div>

  <div class="animated-grid"></div>
</div>

<style>
  .slide-hero {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .slide-background {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 50%, rgba(30, 58, 138, 0.15) 0%, transparent 50%);
    z-index: 1;
  }

  .slide-content {
    position: relative;
    z-index: 2;
    max-width: 1200px;
    padding: var(--spacing-content);
    text-align: center;
  }

  .slide-header {
    margin-bottom: var(--spacing-header);
  }

  .title {
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: var(--spacing-grid);
    animation: fadeUp 0.8s ease-out;
  }

  .highlight {
    background: linear-gradient(135deg, var(--color-accent-bright), var(--color-electric));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    color: var(--color-electric);
    font-family: var(--font-body);
    font-weight: 500;
    opacity: 0.9;
    animation: fadeUp 0.8s ease-out 0.2s backwards;
  }

  .intro-content {
    display: flex;
    justify-content: center;
    margin-bottom: var(--spacing-header);
    animation: fadeUp 0.8s ease-out 0.4s backwards;
  }

  .author-info {
    padding: var(--spacing-card);
    text-align: left;
    max-width: 500px;
  }

  .author-name {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-neutral-light);
    margin-bottom: var(--spacing-sm);
  }

  .author-role {
    font-size: 1.125rem;
    color: var(--color-electric);
    margin-bottom: var(--spacing-grid);
  }

  .author-context {
    font-size: 1rem;
    color: var(--color-neutral-light);
    opacity: 0.8;
  }

  .cta-section {
    animation: fadeUp 0.8s ease-out 0.6s backwards;
  }

  .animated-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(96, 165, 250, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(96, 165, 250, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    z-index: 0;
    animation: moveGrid 20s linear infinite;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes moveGrid {
    0% {
      transform: translate(0, 0);
    }
    100% {
      transform: translate(50px, 50px);
    }
  }

  @media (max-width: 768px) {
    .slide-content {
      padding: var(--spacing-2xl) var(--spacing-lg);
    }

    .author-info {
      padding: var(--spacing-card);
    }

    .author-name {
      font-size: 1.25rem;
    }

    .author-role {
      font-size: 1rem;
    }
  }
</style>
