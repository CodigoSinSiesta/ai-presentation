---
import Button from '../ui/Button.astro';
---

<div class="slide slide-hero swiper-slide">
  <!-- Animated Background -->
  <div class="hero-background">
    <div class="grid-pattern"></div>
    <div class="particles">
      <div class="particle particle-1"></div>
      <div class="particle particle-2"></div>
      <div class="particle particle-3"></div>
      <div class="particle particle-4"></div>
      <div class="particle particle-5"></div>
    </div>
  </div>

  <!-- Content -->
  <div class="hero-content">
    <div class="hero-text">
      <h1 class="hero-title">Los Riesgos Ocultos de la IA</h1>
      <p class="hero-subtitle">Una exploración profunda de los desafíos que enfrenta la humanidad en la era de la Inteligencia Artificial</p>
      <div class="hero-cta">
        <Button class="btn-large">Explorar los riesgos →</Button>
      </div>
    </div>
  </div>
</div>

<style>
  .slide {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-color: var(--color-base-dark);
  }

  .slide-hero {
    background: radial-gradient(ellipse at 50% 50%, rgba(30, 58, 138, 0.3) 0%, var(--color-base-dark) 70%);
  }

  .hero-background {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  /* Grid Pattern */
  .grid-pattern {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(96, 165, 250, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(96, 165, 250, 0.05) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: moveGrid 20s linear infinite;
    opacity: 0.5;
  }

  @keyframes moveGrid {
    0% {
      background-position: 0 0;
    }
    100% {
      background-position: 50px 50px;
    }
  }

  /* Particles */
  .particles {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .particle {
    position: absolute;
    width: 2px;
    height: 2px;
    background: var(--color-electric);
    border-radius: 50%;
    opacity: 0;
    animation: float 15s infinite ease-in-out;
  }

  .particle-1 {
    left: 10%;
    top: 20%;
    animation-delay: 0s;
  }

  .particle-2 {
    left: 20%;
    top: 60%;
    animation-delay: 2s;
  }

  .particle-3 {
    right: 15%;
    top: 30%;
    animation-delay: 4s;
  }

  .particle-4 {
    right: 25%;
    bottom: 20%;
    animation-delay: 6s;
  }

  .particle-5 {
    left: 50%;
    bottom: 15%;
    animation-delay: 8s;
  }

  @keyframes float {
    0% {
      opacity: 0;
      transform: translateY(0);
    }
    50% {
      opacity: 1;
    }
    100% {
      opacity: 0;
      transform: translateY(-100px);
    }
  }

  /* Content */
  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 900px;
    padding: var(--spacing-3xl);
    animation: slideUp 0.8s ease-out 0.3s both;
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 900;
    letter-spacing: -0.02em;
    margin-bottom: var(--spacing-lg);
    background: linear-gradient(135deg, var(--color-accent-bright), var(--color-electric));
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1.1;
  }

  .hero-subtitle {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2.5vw, 1.375rem);
    color: rgba(250, 249, 246, 0.8);
    margin-bottom: var(--spacing-2xl);
    line-height: 1.6;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-cta {
    display: flex;
    justify-content: center;
    gap: var(--spacing-lg);
    animation: fadeIn 0.8s ease-out 0.6s both;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  :global(.btn-large) {
    padding: 1rem 2.5rem;
    font-size: 1.125rem;
    font-weight: 700;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-content {
      padding: var(--spacing-2xl);
    }

    .grid-pattern {
      background-size: 30px 30px;
    }
  }

  @media (max-width: 480px) {
    .hero-content {
      padding: var(--spacing-lg);
    }

    .hero-cta {
      flex-direction: column;
    }

    :global(.btn-large) {
      width: 100%;
    }
  }
</style>
