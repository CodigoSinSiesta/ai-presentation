---
import Card from '../ui/Card.astro';
import Button from '../ui/Button.astro';
import { Share2, BookOpen, Download } from 'lucide-svelte';
---

<div class="slide slide-cta swiper-slide">
  <div class="slide-background"></div>

  <div class="slide-content">
    <div class="cta-message">
      <h2>El Futuro está en Nuestras Manos</h2>
      <p class="cta-subtitle">La IA es una herramienta poderosa que requiere responsabilidad y transparencia</p>
    </div>

    <div class="takeaways">
      <div class="takeaway-item">
        <div class="takeaway-icon">
          <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <circle cx="50" cy="50" r="40" fill="none" stroke="currentColor" stroke-width="2"/>
            <path d="M40 50 L48 58 L65 40" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <h3>Conciencia</h3>
        <p>Entender los riesgos es el primer paso hacia la tecnología responsable</p>
      </div>

      <div class="takeaway-item">
        <div class="takeaway-icon">
          <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <circle cx="50" cy="50" r="40" fill="none" stroke="currentColor" stroke-width="2"/>
            <path d="M35 50 L50 35 L65 50 M50 35 L50 70" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <h3>Acción</h3>
        <p>Demandemos regulaciones éticas y transparencia en sistemas de IA</p>
      </div>

      <div class="takeaway-item">
        <div class="takeaway-icon">
          <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
            <circle cx="50" cy="50" r="40" fill="none" stroke="currentColor" stroke-width="2"/>
            <path d="M50 30 L50 70 M30 50 L70 50 M35 35 L65 65 M65 35 L35 65" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>
        <h3>Colaboración</h3>
        <p>La regulación de la IA es responsabilidad compartida de todos</p>
      </div>
    </div>

    <div class="cta-buttons">
      <Button variant="primary" class="cta-btn">
        <span class="btn-icon">
          <Share2 size={20} />
        </span>
        Compartir
      </Button>
      <Button variant="secondary" class="cta-btn">
        <span class="btn-icon">
          <BookOpen size={20} />
        </span>
        Más información
      </Button>
      <Button variant="secondary" class="cta-btn">
        <span class="btn-icon">
          <Download size={20} />
        </span>
        Descargar
      </Button>
    </div>

    <div class="closing-message">
      <p>Gracias por explorar los riesgos de la IA conmigo</p>
    </div>
  </div>
</div>

<style>
  .slide {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-color: var(--color-base-dark);
    padding: var(--spacing-2xl);
  }

  .slide-background {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, rgba(30, 58, 138, 0.4) 0%, rgba(96, 165, 250, 0.1) 100%),
      radial-gradient(circle at 50% 0%, rgba(59, 130, 246, 0.15) 0%, transparent 70%);
    z-index: 1;
  }

  .slide-content {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 1000px;
    text-align: center;
  }

  .cta-message {
    margin-bottom: var(--spacing-3xl);
    animation: slideUp 0.8s ease-out;
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .cta-message h2 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    margin-bottom: var(--spacing-md);
    background: linear-gradient(135deg, var(--color-accent-bright), var(--color-electric));
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .cta-subtitle {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: rgba(250, 249, 246, 0.8);
  }

  .takeaways {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-2xl);
    margin-bottom: var(--spacing-3xl);
  }

  .takeaway-item {
    background: rgba(30, 58, 138, 0.2);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: var(--radius-lg);
    padding: var(--spacing-2xl);
    transition: all var(--transition-base);
    animation: scaleUp 0.6s ease-out both;
  }

  .takeaway-item:nth-child(1) {
    animation-delay: 0.1s;
  }
  .takeaway-item:nth-child(2) {
    animation-delay: 0.2s;
  }
  .takeaway-item:nth-child(3) {
    animation-delay: 0.3s;
  }

  @keyframes scaleUp {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .takeaway-item:hover {
    background: rgba(30, 58, 138, 0.35);
    border-color: rgba(96, 165, 250, 0.4);
    transform: translateY(-8px);
    box-shadow: 0 12px 32px rgba(59, 130, 246, 0.2);
  }

  .takeaway-icon {
    width: 60px;
    height: 60px;
    margin: 0 auto var(--spacing-lg);
    color: var(--color-electric);
  }

  .takeaway-item h3 {
    font-size: 1.25rem;
    color: var(--color-accent-bright);
    margin-bottom: var(--spacing-md);
  }

  .takeaway-item p {
    font-size: 0.95rem;
    color: rgba(250, 249, 246, 0.75);
  }

  .cta-buttons {
    display: flex;
    justify-content: center;
    gap: var(--spacing-lg);
    flex-wrap: wrap;
    margin-bottom: var(--spacing-2xl);
    animation: fadeIn 0.8s ease-out 0.4s both;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .cta-btn {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
  }

  .btn-icon {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .closing-message {
    margin-top: var(--spacing-2xl);
    animation: fadeUp 0.8s ease-out 0.6s both;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .closing-message p {
    font-size: 1.1rem;
    color: rgba(250, 249, 246, 0.7);
    font-style: italic;
  }

  @media (max-width: 768px) {
    .slide {
      padding: var(--spacing-xl);
    }

    .takeaways {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }

    .cta-buttons {
      flex-direction: column;
    }

    .cta-btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
