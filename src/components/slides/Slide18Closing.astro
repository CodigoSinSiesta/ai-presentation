---
---

<div class="slide slide-closing swiper-slide">
  <div class="slide-background"></div>

  <div class="slide-content">
    <div class="slide-header">
      <h2 class="title">Qué Funciona & Plan</h2>
      <p class="subtitle">Adopción gradual en 4 semanas</p>
    </div>

    <div class="closing-content">
      <div class="works-vs-fails">
        <div class="works-card card-glass">
          <h3>✅ Qué Funciona</h3>
          <ul>
            <li><strong>Tareas mecánicas:</strong> Boilerplate, tests unitarios, migrations</li>
            <li><strong>Guardrails estrictos:</strong> Pre-commit hooks, validación automática</li>
            <li><strong>Review híbrido:</strong> IA detecta bugs obvios, humano valida lógica</li>
            <li><strong>Stack PRs:</strong> 200-400 LOC máximo por PR</li>
            <li><strong>Prompts estructurados:</strong> Contexto + tarea + limitaciones + validación</li>
            <li><strong>Métricas:</strong> Track churn, PR size, review time, bugs</li>
          </ul>
        </div>

        <div class="fails-card card-glass">
          <h3>❌ Qué NO Funciona</h3>
          <ul>
            <li><strong>Confiar ciegamente:</strong> IA genera código sin supervisión</li>
            <li><strong>PRs masivos:</strong> 1000+ LOC generados y mergeados sin review</li>
            <li><strong>Sin tests:</strong> Código en prod sin validación automática</li>
            <li><strong>Ignorar security:</strong> No revisar vulnerabilidades conocidas</li>
            <li><strong>Vibe coding puro:</strong> Generar y mergear sin arquitectura</li>
            <li><strong>Sin rollback plan:</strong> Deploy sin monitoreo o canary</li>
          </ul>
        </div>
      </div>

      <div class="adoption-plan card-glass">
        <h3>🗓️ Plan de Adopción (4 Semanas)</h3>
        <div class="timeline">
          <div class="week">
            <div class="week-number">S1</div>
            <div class="week-content">
              <h4>Setup Inicial</h4>
              <ul>
                <li>Instalar pre-commit hooks (Husky)</li>
                <li>Configurar linters + security scanners</li>
                <li>Definir prompt templates para el equipo</li>
                <li>Baseline de métricas (PR size, review time)</li>
              </ul>
            </div>
          </div>

          <div class="week">
            <div class="week-number">S2</div>
            <div class="week-content">
              <h4>Piloto Controlado</h4>
              <ul>
                <li>2-3 developers prueban IA en tasks no críticos</li>
                <li>Implementar Stack PRs en features nuevas</li>
                <li>Review con 4R Framework (Risk, Readability, Reliability, Resilience)</li>
                <li>Documentar learnings y ajustar guardrails</li>
              </ul>
            </div>
          </div>

          <div class="week">
            <div class="week-number">S3</div>
            <div class="week-content">
              <h4>Escalado</h4>
              <ul>
                <li>Expandir a 50% del equipo</li>
                <li>Implementar augmented reviewers (CodeRabbit, etc)</li>
                <li>Training session: mejores prácticas y pitfalls</li>
                <li>Revisar métricas vs baseline</li>
              </ul>
            </div>
          </div>

          <div class="week">
            <div class="week-number">S4</div>
            <div class="week-content">
              <h4>Optimización</h4>
              <ul>
                <li>100% del equipo adoptando</li>
                <li>Refinar prompt templates basados en resultados</li>
                <li>Retrospectiva: qué funcionó, qué no</li>
                <li>Definir proceso estándar para el futuro</li>
              </ul>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>

<style>
  .slide-closing {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .slide-background {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 50% 50%, rgba(59, 130, 246, 0.12) 0%, transparent 60%),
      radial-gradient(circle at 30% 70%, rgba(34, 197, 94, 0.08) 0%, transparent 50%);
    z-index: 1;
  }

  .slide-content {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    width: 100%;
    padding: var(--spacing-content);
    animation: fadeUp 0.8s ease-out;
  }

  .slide-header {
    text-align: center;
    margin-bottom: var(--spacing-header);
  }

  .title {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 800;
    margin-bottom: var(--spacing-grid);
  }

  .subtitle {
    font-size: clamp(1.125rem, 2.5vw, 1.5rem);
    color: var(--color-electric);
    font-weight: 500;
  }

  .works-vs-fails {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-grid);
    margin-bottom: var(--spacing-grid);
  }

  .works-card, .fails-card {
    padding: var(--spacing-card);
  }

  .works-card {
    border-left: 4px solid #22c55e;
  }

  .fails-card {
    border-left: 4px solid #ef4444;
  }

  h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: var(--spacing-grid);
  }

  ul {
    list-style: none;
    padding: 0;
  }

  ul li {
    padding: var(--spacing-xs) 0 var(--spacing-xs) var(--spacing-lg);
    position: relative;
    font-size: 0.85rem;
    opacity: 0.85;
  }

  .works-card ul li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: #22c55e;
    font-weight: 900;
  }

  .fails-card ul li::before {
    content: '✗';
    position: absolute;
    left: 0;
    color: #ef4444;
    font-weight: 900;
  }

  ul li strong {
    color: var(--color-electric);
  }

  .adoption-plan {
    padding: var(--spacing-card);
    margin-bottom: var(--spacing-grid);
  }

  .adoption-plan h3 {
    text-align: center;
    margin-bottom: var(--spacing-grid);
  }

  .timeline {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--spacing-grid);
  }

  .week {
    background: rgba(10, 22, 40, 0.4);
    border-radius: var(--radius-md);
    padding: var(--spacing-md);
    border-top: 3px solid var(--color-electric);
  }

  .week-number {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-electric), var(--color-accent-bright));
    color: var(--color-base-dark);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 900;
    font-family: var(--font-display);
    margin-bottom: var(--spacing-grid);
    font-size: 1.125rem;
  }

  .week-content h4 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: var(--spacing-sm);
    color: var(--color-electric);
  }

  .week-content ul li {
    font-size: 0.75rem;
    padding: var(--spacing-xs) 0 var(--spacing-xs) var(--spacing-md);
  }

  .week-content ul li::before {
    content: '•';
    color: var(--color-electric);
    font-size: 1rem;
  }

  .key-metrics {
    padding: var(--spacing-card);
    margin-bottom: var(--spacing-grid);
  }

  .key-metrics h3 {
    text-align: center;
    margin-bottom: var(--spacing-grid);
  }

  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-grid);
  }

  .metric {
    display: flex;
    align-items: center;
    gap: var(--spacing-grid);
    padding: var(--spacing-md);
    background: rgba(10, 22, 40, 0.4);
    border-radius: var(--radius-md);
  }

  .metric-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .metric p {
    font-size: 0.85rem;
    opacity: 0.9;
  }

  .metric strong {
    color: var(--color-electric);
  }

  .final-message {
    padding: var(--spacing-card);
    margin-bottom: var(--spacing-grid);
    text-align: center;
  }

  .final-message h3 {
    margin-bottom: var(--spacing-grid);
  }

  .quote {
    font-size: 1.25rem;
    font-style: italic;
    line-height: 1.6;
    margin-bottom: var(--spacing-grid);
    color: var(--color-electric);
  }

  .framework-reminder {
    padding: var(--spacing-lg);
    background: rgba(59, 130, 246, 0.1);
    border-radius: var(--radius-md);
    border: 2px solid var(--color-electric);
  }

  .framework-reminder p {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: var(--spacing-grid);
  }

  .rs {
    display: flex;
    justify-content: center;
    gap: var(--spacing-grid);
    flex-wrap: wrap;
  }

  .r {
    padding: var(--spacing-sm) var(--spacing-lg);
    background: linear-gradient(135deg, var(--color-electric), var(--color-accent-bright));
    color: var(--color-base-dark);
    font-weight: 900;
    font-family: var(--font-display);
    border-radius: var(--radius-md);
    font-size: 0.95rem;
  }

  .cta {
    text-align: center;
    padding: var(--spacing-2xl) 0;
  }

  .cta-text {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-electric);
    margin-bottom: var(--spacing-sm);
  }

  .cta-subtitle {
    font-size: 1.125rem;
    opacity: 0.8;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 1024px) {
    .works-vs-fails, .metrics-grid {
      grid-template-columns: 1fr;
    }

    .timeline {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .slide-content {
      padding: var(--spacing-2xl) var(--spacing-lg);
    }

    .timeline {
      grid-template-columns: 1fr;
    }

    .works-card, .fails-card, .adoption-plan, .key-metrics, .final-message {
      padding: var(--spacing-card);
    }
  }
</style>
