---
---

<div class="slide slide-security swiper-slide">
  <div class="slide-background"></div>

  <div class="slide-content">
    <div class="slide-header">
      <h2 class="title">Riesgos de Seguridad</h2>
      <p class="subtitle">30% del código generado por IA contiene vulnerabilidades</p>
    </div>

    <div class="security-content">
      <div class="stat-highlight card-glass">
        <div class="stat-number">30%</div>
        <p class="stat-description">
          del código generado por IA presenta vulnerabilidades de seguridad críticas
        </p>
      </div>

      <div class="vulnerabilities-grid">
        <div class="vuln-card card-glass">
           <div class="vuln-icon">
             <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
               <path d="M4 7h16"></path>
              <path d="M5 7l1 12a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2l1-12"></path>
              <path d="M9 7V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v3"></path>
              <path d="M10 11v6"></path>
              <path d="M14 11v6"></path>
            </svg>
          </div>
          <h3>Inyección SQL</h3>
          <p>Concatenación directa de inputs en queries</p>
          <div class="examples">
            <code>String interpolation en SQL</code>
            <code>Sin prepared statements</code>
          </div>
        </div>

        <div class="vuln-card card-glass">
           <div class="vuln-icon">
             <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
               <circle cx="12" cy="12" r="10"></circle>
              <path d="M12 8v4"></path>
              <path d="M12 16h.01"></path>
            </svg>
          </div>
          <h3>Cross-Site Scripting</h3>
          <p>Renderizado inseguro de datos de usuario</p>
          <div class="examples">
            <code>HTML directo sin sanitizar</code>
            <code>Falta de escape de caracteres</code>
          </div>
        </div>

        <div class="vuln-card card-glass">
           <div class="vuln-icon">
             <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
               <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"></path>
            </svg>
          </div>
          <h3>Exposición de Secrets</h3>
          <p>Hardcoding de credenciales en el código</p>
          <div class="examples">
            <code>API keys en el código</code>
            <code>Passwords hardcodeadas</code>
          </div>
        </div>

        <div class="vuln-card card-glass">
           <div class="vuln-icon">
             <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
               <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
            </svg>
          </div>
          <h3>Auth Débil</h3>
          <p>Implementaciones inseguras de autenticación</p>
          <div class="examples">
            <code>Sin rate limiting</code>
            <code>Tokens sin expiración</code>
          </div>
        </div>
      </div>

      <div class="warning-box card-glass">
        <h3>⚠️ Regla de Oro</h3>
        <p>
          Nunca confíes ciegamente en código generado por IA.<br />
          <strong>Siempre revisa implementaciones de seguridad críticas.</strong>
        </p>
      </div>
    </div>
  </div>
</div>

<style>
  .slide-security {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .slide-background {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 30% 30%, rgba(239, 68, 68, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 70% 70%, rgba(251, 146, 60, 0.08) 0%, transparent 50%);
    z-index: 1;
  }

  .slide-content {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    width: 100%;
    padding: var(--spacing-content);
    animation: fadeUp 0.8s ease-out;
  }

   .slide-header {
     text-align: center;
     margin-bottom: var(--spacing-md);
   }

   .title {
     font-size: clamp(2.5rem, 5vw, 3.5rem);
     font-weight: 800;
     margin-bottom: var(--spacing-sm);
     color: #ef4444;
   }

   .subtitle {
     font-size: clamp(0.9rem, 2vw, 1.125rem);
     color: #f87171;
     font-weight: 600;
   }

   .stat-highlight {
     text-align: center;
     padding: var(--spacing-card);
     margin-bottom: var(--spacing-sm);
     border: 2px solid rgba(239, 68, 68, 0.3);
   }

   .stat-number {
     font-size: 3.5rem;
     font-weight: 900;
     font-family: var(--font-display);
     background: linear-gradient(135deg, #ef4444, #f87171);
     -webkit-background-clip: text;
     -webkit-text-fill-color: transparent;
     background-clip: text;
     line-height: 1;
     margin-bottom: var(--spacing-sm);
   }

   .stat-description {
     font-size: 1rem;
     opacity: 0.9;
   }

    .vulnerabilities-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: var(--spacing-sm);
      margin-bottom: var(--spacing-md);
    }

   .vuln-card {
     padding: var(--spacing-sm);
     text-align: center;
     transition: all var(--transition-base);
     border: 1px solid rgba(239, 68, 68, 0.2);
   }

  .vuln-card:hover {
    transform: translateY(-6px);
    border-color: rgba(239, 68, 68, 0.4);
    box-shadow: 0 8px 25px rgba(239, 68, 68, 0.2);
  }

   .vuln-icon {
     width: 48px;
     height: 48px;
     margin: 0 auto var(--spacing-sm);
     display: flex;
     align-items: center;
     justify-content: center;
     border-radius: var(--radius-md);
     background: rgba(239, 68, 68, 0.1);
     color: #ef4444;
   }

   .vuln-card h3 {
     font-size: 1rem;
     font-weight: 700;
     margin-bottom: var(--spacing-xs);
   }

    .vuln-card p {
      font-size: 0.8rem;
      opacity: 0.85;
      margin-bottom: var(--spacing-xs);
    }

   .examples {
     display: flex;
     flex-direction: column;
     gap: var(--spacing-sm);
   }

   .examples code {
     font-family: var(--font-mono);
     font-size: 0.7rem;
     background: rgba(10, 22, 40, 0.6);
     padding: var(--spacing-xs) var(--spacing-xs);
     border-radius: var(--radius-sm);
     border-left: 2px solid #ef4444;
     text-align: left;
     display: block;
     overflow-x: auto;
     white-space: nowrap;
   }

  .warning-box {
    padding: var(--spacing-card);
    text-align: center;
    border: 2px solid rgba(251, 146, 60, 0.4);
    background: rgba(251, 146, 60, 0.05);
  }

   .warning-box h3 {
     font-size: 1.25rem;
     font-weight: 700;
     color: #fb923c;
     margin-bottom: var(--spacing-sm);
   }

   .warning-box p {
     font-size: 0.95rem;
     line-height: 1.5;
   }

  .warning-box strong {
    color: var(--color-electric);
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 1024px) {
    .vulnerabilities-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .slide-content {
      padding: var(--spacing-2xl) var(--spacing-lg);
    }

    .vulnerabilities-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-grid);
    }

    .stat-number {
      font-size: 3.5rem;
    }

    .vuln-card {
      padding: var(--spacing-lg);
    }
  }
</style>
